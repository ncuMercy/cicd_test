# Use Node.js 22 LTS on Alpine Linux
FROM node:22-alpine

WORKDIR /workspace

# Install common tools with apk (Alpine package manager)
RUN apk add --no-cache \
    bash \
    git \
    curl \
    vim 

CMD ["bash"]
